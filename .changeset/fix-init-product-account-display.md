---
"playground-cli": patch
---

Fix `dot init` so the product account row shows the actual signer account used by deploy, keep Bulletin out of the mobile allowance request, show the Bulletin authorization faucet for the locally cached Bulletin account, and let `dot logout` recover from stale sessions missing the product-derivation root key.
